# Application
APP_ENV=${APP_ENV}
DEBUG=${DEBUG}
HOST=${HOST}
PORT=${PORT}

# Qdrant
QDRANT_HOST=${QDRANT_HOST}
QDRANT_PORT=${QDRANT_PORT}
QDRANT_COLLECTION_NAME=${QDRANT_COLLECTION_NAME}
QDRANT_SHOP_COLLECTION_NAME=${QDRANT_SHOP_COLLECTION_NAME}
QDRANT_USE_HTTPS=${QDRANT_USE_HTTPS}
QDRANT_PREFER_GRPC=${QDRANT_PREFER_GRPC}

# Redis
REDIS_HOST=${REDIS_HOST}
REDIS_PORT=${REDIS_PORT}
REDIS_DB=${REDIS_DB}
REDIS_PASSWORD=${REDIS_PASSWORD}
REDIS_MAX_CONNECTIONS=${REDIS_MAX_CONNECTIONS}

# HuggingFace
HF_HOME=${HF_HOME}
HF_TOKEN=${HF_TOKEN}

# Models
EMBEDDING_MODEL=${EMBEDDING_MODEL}
UPSTAGE_API_KEY=${UPSTAGE_API_KEY}
CAPTION_MODEL=${CAPTION_MODEL}
GEMINI_API_KEY=${GEMINI_API_KEY}
GEMINI_MODEL=${GEMINI_MODEL}
VTON_MODEL=${VTON_MODEL}
OPENAI_API_KEY=${OPENAI_API_KEY}
OPENAI_CHAT_MODEL=${OPENAI_CHAT_MODEL}
LLM_TIMEOUT=${LLM_TIMEOUT}
LLM_MAX_RETRIES=${LLM_MAX_RETRIES}

# Mock Analyzer
USE_MOCK_ANALYZER=${USE_MOCK_ANALYZER}

# Langfuse
LANGFUSE_SECRET_KEY=${LANGFUSE_SECRET_KEY}
LANGFUSE_PUBLIC_KEY=${LANGFUSE_PUBLIC_KEY}
LANGFUSE_HOST=${LANGFUSE_HOST}
LANGFUSE_DEBUG=${LANGFUSE_DEBUG}

# Naver API
NAVER_CLIENT_ID=${NAVER_CLIENT_ID}
NAVER_CLIENT_SECRET=${NAVER_CLIENT_SECRET}

# Backend Internal API
BACKEND_INTERNAL_URL=${BACKEND_INTERNAL_URL}
BACKEND_INTERNAL_API_KEY=${BACKEND_INTERNAL_API_KEY}

# Kafka
KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS}
KAFKA_ANALYZE_CONSUMER_GROUP=${KAFKA_ANALYZE_CONSUMER_GROUP}
KAFKA_MAX_CONCURRENT_TASKS=${KAFKA_MAX_CONCURRENT_TASKS}
